# CLAUDE.md

Read `AGENTS.md` before doing anything in this repo. That file — not this
one — is the actual entrypoint: what this repo is, where the real
conventions live, the two rules that matter most, and the docs map. This
file exists only because Claude Code looks for `CLAUDE.md` specifically;
treat it as a pointer, not a second copy of the rules.
